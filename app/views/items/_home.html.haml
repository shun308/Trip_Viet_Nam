%table
  %thead
    %tr
      %th 登録
      %th 日本語
      %th ベトナム語
      %th 発音
  %tbody
  - @items.each do |item|
    %tr
      / お気に入り登録ボタン ルートに従いパスを記述、POSTメソッドに指定
      %td= link_to "登録", add_item_path(item), method: :post
      / 本の詳細へのリンク
      %td= item.japanese
      %td= item.vietnamese
      %td= item.intonation
= paginate(@items)
 /ページネーション
