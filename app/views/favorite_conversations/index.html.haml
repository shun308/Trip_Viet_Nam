= render "shared/main_page"
%body.top
  %table.top1
    .new_post お気に入り
    =link_to "戻る", root_path, class:"btn btn-secondary", style:"margin:30px;"
    -# = render "shared/flash"
    %thead.top3
    - if @favorite_conversations.length > 0
      %tr.top6
        .fas.fa-star{style:"color:#00acee;"}
        = @favorite_conversations.length
        個のお気に入りがあります
        %th.top2 日本語
        %th.top2 ベトナム語
        %th.top2 発音
        %th.top2 削除
      %tbody.top5
      - @favorite_conversations.each do |favorite_conversation|
        - conversation = Conversation.find(favorite_conversation.conversation_id)
        %tr.top6
          %td.top4= conversation.japanese_conversation
          %td.top4= conversation.vietnamese_conversation
          %td.top4= conversation.intonation_conversation
          %td.top4= link_to "", favorite_conversation_path(favorite_conversation), method: :delete, class:"fas fa-bomb",style:"color:black;"
    - else
      .fas.fa-star{style:"color:#00acee;"} 
      「お気に入り」した会話 文章はありません