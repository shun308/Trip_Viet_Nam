= render "shared/main_page"
%body.top
  %table.bottom
    .new_post お気に入り
    =link_to "戻る", root_path, class:"btn btn-dark", style:"margin:30px;"
    -# = render "shared/flash"
    %thead
    - if @favorites.length > 0
      %tr
        .fas.fa-star{style:"color:gold;"}
        = @favorites.length
        個のお気に入りがあります
        %th.bottom 日本語
        %th.bottom ベトナム語
        %th.bottom 発音
        %th.bottom 削除
      %tbody
      - @favorites.each do |favorite|
        - item = Item.find(favorite.item_id)
        %tr
          %td.bottom1= item.japanese
          %td.bottom1= item.vietnamese
          %td.bottom1= item.intonation
          %td.bottom1= link_to "", favorite_path(favorite), method: :delete, class:"fas fa-bomb",style:"color:black;"
    - else
      .fas.fa-star{style:"color:gold;"} 
      「お気に入り」した単語はありません